name: dataclaw_test
version: "1.0.0"
config-version: 2

profile: dataclaw_test

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  dataclaw_test:
    staging:
      +schema: staging
      +materialized: view
    mart:
      +schema: mart
      +materialized: table
